<?xml version="1.0" encoding="UTF-8"?>
<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:section title="${%JobAgentLauncher}">
        <f:entry field="gitUrl" title="${%GitUrl}">
            <f:textbox/>
        </f:entry>
        <f:entry field="gitRevision" title="${%GitRevision}">
            <f:textbox/>
        </f:entry>
        <f:entry field="label" title="${%Label}">
            <f:readOnlyTextbox/>
        </f:entry>
        <f:entry field="image" title="${%Image}">
            <f:textbox/>
        </f:entry>
        <f:entry field="provisionJob" title="${%ProvisionJob}">
            <f:textbox/>
        </f:entry>
        <f:entry field="decomissionJob" title="${%DecomissionJob}">
            <f:textbox/>
        </f:entry>
        <f:entry field="revision" title="${%Revision}">
            <f:textbox/>
        </f:entry>
        <!-- This property is included only if used directly. Causes JENKINS-45895 in the case of includes otherwise -->
        <!-- <j:if test="${descriptor.workDirSupported}"> 
            <f:property title="${%Enable work directory}" field="workDirSettings" />
        </j:if>
        <f:advanced>
            <f:entry title="${%Tunnel connection through}" help="/help/system-config/master-slave/jnlp-tunnel.html">
                <f:textbox field="tunnel"/>
            </f:entry>
            <f:entry title="${%JVM options}" field="vmargs">
                <f:textbox />
            </f:entry>
        </f:advanced> -->
    </f:section>
</j:jelly>
